# AWS S3 Configuration for mask uploads
S3_BUCKET_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=

# Example usage:
# 1. Copy this file to .env
# 2. Replace the values with your actual AWS credentials
# 3. Make sure your S3 bucket exists and has appropriate permissions
# 4. The API will automatically upload segmentation masks to S3
